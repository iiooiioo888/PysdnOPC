- 每个 channel 配置项统一包含 enabled、allow_from 白名单以及 provider 特定的凭字段（token/app_id/client_id 等），未填写即 deny-by-default。
- 外部 agent 与 native subagent 的配置采用扁平键值结构，并通过 preferred_order 列表声明优先级顺序。
- 敏感凭据一律以空字符串占位，注释中提示通过 api_key_env 环境变量注入，避免将密钥提交到仓库。
- 公司组织配置使用 schema_version 字段标记版本，新增字段需递增版本号以保持向后兼容。